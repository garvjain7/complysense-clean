This is a functional-completion pass, NOT a styling pass. The goal here is
making broken/fake/unverified things real. Do not apply visual polish,
color changes, or the UI improvement work from the separate UI prompt in
this pass — stay focused on function.

════════════════════════════════════════════════════════════════
1. IMPLEMENT REAL STUBS (currently render nothing functional)
════════════════════════════════════════════════════════════════

Quick check first for each file: confirm it's still a stub before doing
work — if it's already been implemented since this was last reviewed,
skip it and report "already implemented."

1a. frontend/src/pages/compliance/Notifications.tsx
    Currently a one-line placeholder with zero API calls. The backend
    for this is fully built and confirmed working:
      GET /api/v1/notifications?limit=N&unread=true/false
        -> { notifications: [...], unread_count: N }
      PATCH /api/v1/notifications/read-all -> { message: "ok" }
    Build a real page: fetch and list notifications (title, message,
    notification_type, created_at, is_read), a "Mark all read" button
    calling the PATCH endpoint, loading/error/empty states using the
    existing shared components (Loading, ErrorState, EmptyState — same
    pattern as compliance/EvidenceQueue.tsx). Clicking a notification
    should navigate based on related_entity_type/related_entity_id if
    present — check how Topbar.tsx's NotifItem already does this routing
    (getNotifBasePath + paths mapping) and reuse/mirror that same routing
    logic here rather than inventing new navigation rules.

1b. frontend/src/pages/vendor/NewVendor.tsx
    Currently a one-line placeholder. Build a real form matching the
    backend contract (POST /api/v1/vendors):
      vendor_name (required), product_name, vendor_category (select:
      cloud/saas/payment/data_processor/security/other), processing_location,
      dpa_available (checkbox), model_training_allowed (checkbox),
      contract_expiry_date (date), contact_email
    On success, navigate to /vendor/vendors/{new_vendor_id} (the detail
    page, which already exists and works — VendorDetail.tsx). Match the
    form styling/structure already used in institution-admin/Departments.tsx
    or Tenants.tsx's "Add" modal pattern for consistency (form-group,
    form-label, form-input classes), but as a full page, not a modal,
    since this route is a dedicated page.

1c. frontend/src/pages/vendor/ExpiryTracker.tsx
    Currently a one-line placeholder. Build a real page listing vendors
    with an upcoming or past contract_expiry_date, sourced from
    GET /api/v1/vendors (same endpoint vendor/Dashboard.tsx already uses).
    Group or sort by urgency (already expired / expiring in 30 days /
    expiring in 90 days / later), reusing the same expiringSoon logic
    already written in vendor/Dashboard.tsx (90-day window check) as the
    basis — don't reinvent that calculation, factor it out into a shared
    helper if convenient, or just duplicate the same logic consistently.

1d. frontend/src/pages/dept/SelfAssessment.tsx
    This is the most important one — it currently LOOKS fully functional
    but handleSubmit only sets local state and calls no API at all. This
    is actively misleading users into thinking their submission was saved.
    First: check if a backend endpoint for self-assessments exists at all
    (search backend/app/routers/ for anything matching "self_assessment"
    or "self-assessment" or similar). 
      - If NO such endpoint exists: do not fabricate a fake save. Instead,
        add a clearly visible notice on the page itself (not just a
        console warning) stating this feature is not yet connected to
        the backend, and report this back explicitly as a blocker needing
        a backend endpoint before this can be completed.
      - If an endpoint DOES exist: wire the form to actually call it,
        remove the fake local-only success state, and use the real API
        response to confirm success/failure with proper error handling.

════════════════════════════════════════════════════════════════
2. FIX MISLEADING/DEAD-END UI ELEMENTS
════════════════════════════════════════════════════════════════

2a. frontend/src/components/shared/Sidebar.tsx
    "Profile Settings" menu item navigates to `/profile`, but no such
    route exists anywhere in the route files (AppRouter.tsx or any role
    route file). Either:
      - remove this menu item entirely until a profile page exists, or
      - if backend/app/routers/auth.py's PATCH /auth/me (update own
        profile) is meant to back a real profile page, flag this back
        to me as a missing page to build rather than silently deleting
        the menu item.
    Do not guess — report which of these two you find and did, and why.

2b. frontend/src/components/shared/Sidebar.tsx
    The institution name shown under the brand logo currently renders
    the raw institution_id (a UUID), not the institution's actual name:
      {user.role_name !== "Super Admin" && (
        <div className="sidebar-institution-name">
          {user.institution_id}
        </div>
      )}
    The AuthUser type (frontend/src/types/auth.ts) does not currently
    carry an institution_name field. Check backend/app/schemas/auth.py's
    UserContext model — if it also lacks institution_name, this needs a
    backend change (add institution_name to the UserContext response,
    joining from the institutions table) before the frontend can display
    it correctly. Report this as a backend-dependent fix if that's the
    case, rather than working around it with another API call from the
    sidebar itself (which would fire on every page load unnecessarily).

════════════════════════════════════════════════════════════════
3. REMOVE HARDCODED CONTENT THAT SHOULD BE DYNAMIC
════════════════════════════════════════════════════════════════

3a. frontend/src/pages/dept/TaskWizard.tsx
    The "Suggested completion checklist" (3 bullet points under the task
    details) is static hardcoded JSX text, identical for every task
    regardless of what the task actually is. Check whether task_description
    or any other field on the task object could reasonably drive this
    content instead. If there's no real per-task data to base a checklist
    on, leave the content as-is but relabel it clearly as generic guidance
    (e.g. "General submission checklist") rather than implying it's
    task-specific — do not remove the checklist, just stop presenting
    generic advice as if it were tailored to this task.

3b. frontend/src/pages/institution-admin/Dashboard.tsx
    handleGenerateRiskHeatmap() has this duplicate/dead fallback:
      const risks = res.data?.ai_risks ?? res.data?.ai_risks ?? [];
    Fix to:
      const risks = res.data?.ai_risks ?? [];

════════════════════════════════════════════════════════════════
4. VERIFY UNCONFIRMED API ENDPOINTS AGAINST BACKEND
════════════════════════════════════════════════════════════════

These frontend pages call backend routes that have not yet been confirmed
to exist with matching paths/response shapes. For each, open the
corresponding backend router file and check. Do NOT fix anything here yet
— only report findings (path exists / path missing / response shape
mismatch) so a decision can be made afterward.

  - frontend/src/pages/compliance/Policies.tsx,
    frontend/src/pages/policy/Inbox.tsx,
    frontend/src/pages/policy/History.tsx,
    frontend/src/pages/policy/PolicyReview.tsx
    all call /api/v1/policies* — check against backend/app/routers/policies.py
    (confirm this file exists and its routes match: GET "", POST "",
    GET "/{id}", PATCH "/{id}", GET "/reports", POST "/reports/generate")

  - frontend/src/pages/super-admin/AuditTrail.tsx,
    frontend/src/pages/super-admin/TenantDetail.tsx,
    frontend/src/pages/super-admin/Dashboard.tsx,
    frontend/src/pages/auditor/Observations.tsx
    call /api/v1/audit/logs, /api/v1/audit/recent, /api/v1/audit/observations
    — check against backend/app/routers/audit.py (plain, non-AI version —
    NOT ai/audit.py) and confirm these routes exist with matching params
    (page, limit, institution_id, action_type, user_search, from_date,
    to_date for /audit/logs; limit for /audit/recent)

  - frontend/src/pages/institution-admin/Users.tsx,
    frontend/src/pages/super-admin/TenantDetail.tsx
    call /api/v1/users*, /api/v1/users/invite, /api/v1/users/{id}/role,
    /api/v1/users/{id}/unlock, /api/v1/users/{id}/status — check against
    backend/app/routers/users.py

Report back a simple table: file | endpoint called | status (confirmed /
missing / mismatched — describe the mismatch) for every one of the above.
